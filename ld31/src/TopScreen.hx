using Math;
class TopScreen extends S{
	public function new(s) 	{
		super(s);
		
		new h2d.Bitmap( h2d.Tile.fromColor(0x7DFFFF00,scene.width.round(),scene.height.round()$), mask );
	}
	
	public override function update(tmod) {
		super.update(tmod);
	}
}
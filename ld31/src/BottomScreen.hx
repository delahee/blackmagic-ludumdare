import T;

class BottomScreen extends S{
	public function new(s) 	{
		super(s);
		new h2d.Bitmap( h2d.Tile.fromColor(0x7FFFFF00,1000,1000), scene );
	}
	
	public override function update(tmod) {
		super.update(tmod);
	}
}